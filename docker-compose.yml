version: "2"
services:
  http-server:
    image: app-fab/http-server
    build: microservices/http-server
    ports:
      - 3000:3000
    volumes:
      - /app/storage
    environment:
      REPOSITORY_PATH: /app/storage  

      
